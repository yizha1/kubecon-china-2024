name: deploy-app

on:
  workflow_dispatch

env:
  RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}        
  CLUSTER_NAME: ${{ vars.AZURE_CLUSTER_NAME }}
  CHART_PATH: ./charts

jobs:
  deploy-myapp-to-k8s:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    - name: Gets K8s context
      uses: azure/aks-set-context@v1
      with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          resource-group: ${{ env.RESOURCE_GROUP }}
          cluster-name: ${{ env.CLUSTER_NAME }}
      id: login

    - name: Bake helm charts
      uses: azure/k8s-bake@v1
      with:
        renderEngine: helm
        helmChart: ${{ env.CHART_PATH }}
        overrides: |
          image.repository=${{ env.APP_REGISTRY }}/${{ env.APP_IMAGE_PROD_REPO }}
          image.tag=${{ evn.APP_IMAGE_PROD_TAG }}
          fluentd.image.repository=${{ env.APP_REGISTRY }}/${{ env.UTILITY_IMAGE_PROD_REPO }}
          fluentd.image.tag=${{ evn.APP_IMAGE_PROD_TAG }}
      id: bake
